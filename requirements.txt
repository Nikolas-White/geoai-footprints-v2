# NOTE: install PyTorch FIRST and SEPARATELY, because it needs the CUDA-specific
# package index:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# Then: pip install -r requirements.txt

# --- core ---
numpy>=1.26,<2.0
pandas>=2.2,<3.0
PyYAML>=6.0
tqdm>=4.66
requests>=2.31

# --- archive handling (fallback if 7-Zip is not installed) ---
py7zr>=0.21

# --- imaging ---
opencv-python>=4.9,<4.11
imageio>=2.34
scikit-image>=0.22
Pillow>=10.2

# --- geospatial ---
rasterio>=1.3.9
shapely>=2.0.3
geopandas>=0.14
pyproj>=3.6
fiona>=1.9

# --- deep learning (torch itself installed separately, see above) ---
segmentation-models-pytorch>=0.3.4
timm>=0.9.16

# --- reporting ---
matplotlib>=3.8
folium>=0.16

# Deliberately NOT albumentations: it pulls in albucore -> stringzilla, which has
# no prebuilt wheel for CPython 3.11 on Windows and therefore requires MSVC build
# tools to install. Augmentations are implemented directly in src/data/augment.py.
